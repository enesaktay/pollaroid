{% extends 'base.html.twig' %}

{% block body %}
    {#<h1>{{ dump(poll) }}</h1>#}

    <h1 class="title is-1">{{ poll.question }}</h1>

    {#{% for answer in poll.answer %}#}
        {#<h5 class="subtitle is-2">{{ answer }}</h5>#}
    {#{% endfor %}#}

    {% if form %}
        {{ form_start(form) }}

        {{ form_end(form) }}
    {% endif %}

{% endblock %}